node_modules
dist
out
out-check
.vite
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.DS_Store
*.log
.env
.env.local
userData
txt.txt
.cloude/
*.tsbuildinfo
.tmp-audio/
.tmp-frames/

# Local MCP config (may contain API keys) — keep out of git
.mcp.json

# scratch: STT validation
test-stt/
capture-speaking.mjs

# Planning / feature docs stay local — only the app README is published
*.md
!/README.md
